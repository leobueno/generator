@(package: String, table : String, columns: Array[Map[String, String]])

package @package

case class @table (
    @for(column <- columns) {

    }

    {{#columns}}
      {{column}} : {{scalaType}}
    {{/columns}}
)
